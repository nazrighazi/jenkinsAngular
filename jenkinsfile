node{

  stage('Checkout SCM'){
    git branch: 'main', url: ''
    
    }
  
  stage('Install node modules'){
    sh "npm install"
    
    }
    
  stage("Build){
    sh "npm run build"
    
    }
    
}
