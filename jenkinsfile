node{

  stage('Checkout SCM'){
    git branch: 'main', url: 'https://github.com/nazrighazi/jenkinsAngular.git'
     
    }
  
  stage('Install node modules'){
    sh "npm install"
    
    }
    
  stage('Build'){
    sh "npm run build"
    
    }
    
}
